import React, {useState} from 'react';
import {View, Text, Image} from 'react-native';

const PetProfile = (props) => {
  return (
    <View>
      <Text>Pet PetProfile</Text>
    </View>
  );
}

export default PetProfile;